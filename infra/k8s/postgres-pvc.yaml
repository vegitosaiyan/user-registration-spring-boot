apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: user-registration
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # For local development with Docker Desktop or Minikube, use default storage class
  # storageClassName: standard  # Uncomment if needed
